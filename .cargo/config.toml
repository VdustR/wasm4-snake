# Default target for cargo build (not applied to cargo test)
# Use: cargo build --release
# For tests: cargo test (runs on native target)
[build]
target = "wasm32-unknown-unknown"

[alias]
# Convenience alias for running tests on native target
test-native = "test --target aarch64-apple-darwin"

[target.wasm32-unknown-unknown]
rustflags = [
    # Import memory from WASM-4
    "-C", "link-arg=--import-memory",
    "-C", "link-arg=--initial-memory=65536",
    "-C", "link-arg=--max-memory=65536",

    # Temporary workaround for #255 issue.
    # Reserve 8192 bytes of Rust stack space, offset from 6560.
    # Bump this value, 16-byte aligned, if the framebuffer gets corrupted.
    "-C", "link-arg=-zstack-size=14752",
]
